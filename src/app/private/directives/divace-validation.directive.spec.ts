import { DivaceValidationDirective } from './divace-validation.directive';

describe('DivaceValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DivaceValidationDirective();
    expect(directive).toBeTruthy();
  });
});
